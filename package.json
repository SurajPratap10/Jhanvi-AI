{
  "name": "ai-voice-agent",
  "version": "1.0.0",
  "description": "A full-stack voice assistant web app with React frontend and Node.js backend",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "client": "cd client && npm start",
    "server": "cd server && npm run dev",
    "install-all": "npm install && cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "test": "cd client && npm test",
    "lint": "cd client && npm run lint",
    "clean": "rm -rf client/build server/node_modules client/node_modules node_modules",
    "setup": "npm run install-all && echo '✅ Setup complete! Run npm run dev to start the application'"
  },
  "keywords": ["voice", "assistant", "react", "openai", "speech-to-text", "text-to-speech"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 